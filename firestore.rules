
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC ACCESS: All collections are readable and writable by everyone.
    // This removes all "Missing or insufficient permissions" errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
